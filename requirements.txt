# Core downloader
yt-dlp>=2026.7.4

# Windows only: lets yt-dlp mint the PO token that audio-only streams now
# require, so downloads don't fail with HTTP Error 403. Harmless elsewhere.
yt-dlp-getpot-wpc>=1.1.2

# Also required on the system (not pip):
#   ffmpeg  — merges the 1080p video+audio streams and reports the real quality
#   node    — JavaScript runtime yt-dlp needs to solve YouTube's "n" challenge
#             (macOS: deno also works and is auto-detected)
